# Development Space Interface

 * `.sh` - Sourced by the other scripts.
 * `exec` - Custom action.
 * `init` - Obtain sources.
 * `make` - Compile binaries.
 * `test` - Perform testing.
 * `undo` - Delete files.
